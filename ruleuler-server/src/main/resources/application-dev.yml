spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/ruleuler_data?useUnicode=true&characterEncoding=utf-8&autoReconnect=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: asdfg123
    tomcat:
      min-idle: 5
      max-idle: 5
      max-active: 10
      test-on-borrow: true
      test-while-idle: true
      validation-query: SELECT 1
      time-between-eviction-runs-millis: 60000
      min-evictable-idle-time-millis: 300000
      remove-abandoned: true
      remove-abandoned-timeout: 180

  jackson:
    default-property-inclusion: non_null

log4j2:
  formatMsgNoLookups: true

urule:
  debug: true
  console:
    title: RulEuler DEV v@revision@
  admin:
    baseUrl: http://localhost:16009/admin
  repository:
    databasetype: mysql
    datasourcename: datasource
    dir: ${URULE_REPO_DIR:./urule-repo}

admin:
  staticPath: ${ADMIN_STATIC_PATH:}

console:
  staticPath: ${CONSOLE_STATIC_PATH:${user.dir}/ruleuler-console/src/main/resources}

server:
  port: 16009

auth:
  jwt:
    secret: dev-only-secret-key-that-is-at-least-32-bytes!!
    expiration-hours: 24

name:
  dev
